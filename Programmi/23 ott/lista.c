#include <stdio.h>

/* Scrivere un programma che riceva in ingresso un intero e una classe di studenti e costruisce la lista degli studenti più giovani della classe, tenendola */

typedef struct

{
	int num_elem;
	float seq[100];
} lista;

lista calcola(lista classe, int n);
// Restituisce l'elenco dei 100 più giovani

void calcola(lista classe, int n, lista *pl)

int main(){
	
	return 0;
}
